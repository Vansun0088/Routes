import { StyleSheet, Text, View } from "react-native";

export default function Options() {
	return <View style={styles.optionsContainer}></View>;
}

const styles = StyleSheet.create({
	optionsContainer: {
		flex: 1,
		backgroundColor: "yellow",
	},
});
